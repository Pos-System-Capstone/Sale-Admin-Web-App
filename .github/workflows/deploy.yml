name: Upload Website

# This file was made by lord of the cat - meow 
on:
  push:
    branches:
    - main
  workflow_dispatch: 
    inputs: 
     envFile:
      description: Env file to deploy
      type: text
      required: false
      default: '.env.production'

jobs:
  DeployToAWS: 
     uses: meowmeoworg/workflow-support/blob/develop/.github/workflows/DeployReactStaticWebToS3.yml@develop
     with:
      envFile: ${inputs.envFile}
     secrets: inherit
    
